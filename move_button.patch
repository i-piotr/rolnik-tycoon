diff --git a/main.py b/main.py
index a53ff33..fd1d56e 100644
--- a/main.py
+++ b/main.py
@@ -10,14 +10,6 @@ from field import Field
 from menu import Menu
 from farm import Farm
 
-def draw_button(screen, text, rect, button_color, text_color):
-    """Rysuje przycisk na ekranie."""
-    pygame.draw.rect(screen, button_color, rect, border_radius=10)
-    font = pygame.font.Font(None, 30)
-    text_surf = font.render(text, True, text_color)
-    text_rect = text_surf.get_rect(center=rect.center)
-    screen.blit(text_surf, text_rect)
-
 def game_loop(screen, farm, player):
     """Główna pętla gry."""
     # Główna pętla gry
@@ -29,18 +21,12 @@ def game_loop(screen, farm, player):
         farm.draw(screen)
         player.draw_top_bar(screen)
 
-        # Przycisk "Kolejna tura"
-        next_turn_button_rect = pygame.Rect(850, 500, 150, 50)
-        next_turn_button_color = (0, 128, 0)
-        draw_button(screen, "Kolejna tura", next_turn_button_rect, next_turn_button_color, (255, 255, 255))
-
-
         # Obsługa zdarzeń
         for event in pygame.event.get():
             if event.type == pygame.QUIT:
                 running = False
             if event.type == pygame.MOUSEBUTTONDOWN:
-                if next_turn_button_rect.collidepoint(event.pos):
+                if player.next_turn_button_rect and player.next_turn_button_rect.collidepoint(event.pos):
                     farm.next_turn()
 
         # Aktualizacja wyświetlacza
diff --git a/player.py b/player.py
index 98f480c..1b90f4a 100644
--- a/player.py
+++ b/player.py
@@ -1,11 +1,20 @@
 import pygame
 from datetime import date, timedelta
 
+def draw_button(screen, text, rect, button_color, text_color):
+    """Rysuje przycisk na ekranie."""
+    pygame.draw.rect(screen, button_color, rect, border_radius=10)
+    font = pygame.font.Font(None, 30)
+    text_surf = font.render(text, True, text_color)
+    text_rect = text_surf.get_rect(center=rect.center)
+    screen.blit(text_surf, text_rect)
+
 class Player:
     """Klasa reprezentująca gracza."""
     def __init__(self, cash):
         self.cash = cash
         self.current_date = date(2024, 1, 1)
+        self.next_turn_button_rect = None # Przycisk tury
 
     def draw_top_bar(self, screen):
         """Rysuje górny pasek z datą i kasą."""
@@ -22,3 +31,16 @@ class Player:
         cash_text = font.render(f"Kasa: {self.cash:.2f} PLN", True, (255, 255, 0))
         cash_rect = cash_text.get_rect(right=screen.get_width() - 10, centery=bar_height / 2)
         screen.blit(cash_text, cash_rect)
+
+        # Rysowanie przycisku "Kolejna tura"
+        button_width, button_height = 140, 30
+        
+        # Tworzymy prostokąt przycisku i zapisujemy go w obiekcie gracza
+        # aby był dostępny do sprawdzania kliknięć w pętli głównej
+        self.next_turn_button_rect = pygame.Rect(0, 0, button_width, button_height)
+        self.next_turn_button_rect.centery = bar_height / 2
+        self.next_turn_button_rect.right = cash_rect.left - 15 # Pozycja na lewo od kasy
+        
+        # Rysujemy przycisk
+        draw_button(screen, "Kolejna tura", self.next_turn_button_rect, (0, 128, 0), (255, 255, 255))
+